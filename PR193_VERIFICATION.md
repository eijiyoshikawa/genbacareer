# PR #193 求職者ウィザード 動作検証チェックリスト

> このファイルは検証完了後に削除して構いません (Git からも除外可)。
> 各項目を順に確認し、`[ ]` を `[x]` に書き換えながら進めてください。

---

## 🔧 0. 事前準備 (環境チェック)

- [ ] プロジェクトルートに `cd` 済み (`pwd` が `let_kyujin` で終わる)
- [ ] `git status` で未コミット変更がない (あれば stash か commit)
- [ ] `.env` ファイルが存在する (`ls -la .env`)
- [ ] `node --version` が v20 以上
- [ ] `pnpm --version` が動く

```bash
pwd
git status
ls -la .env
node --version
pnpm --version
```

---

## 🌿 1. ブランチ取得

```bash
git fetch origin claude/registration-wizard-phase1
git checkout claude/registration-wizard-phase1
git pull origin claude/registration-wizard-phase1
```

- [ ] `git log --oneline -3` の最新が `fix(wizard): セキュリティ・データ整合性の Critical 4 件を修正`
- [ ] `ls src/lib/registration/phone.ts` でファイルが存在する

```bash
git log --oneline -3
ls src/lib/registration/phone.ts
```

---

## 📦 2. 依存セットアップ

```bash
pnpm install
pnpm prisma generate
```

- [ ] エラーなく完了
- [ ] `Prisma Client (vX.X.X) generated` が出る

---

## 🚀 3. 開発サーバ起動

別ターミナルを開いて:

```bash
pnpm dev
```

- [ ] `✓ Ready in X.Xs` が出る
- [ ] ポート 3000 で起動 (`http://localhost:3000`)
- [ ] 既存ホームページが正常に表示される (http://localhost:3000)

---

## 🔍 4. 入口ページ確認 (`/register/wizard`)

ブラウザ: **http://localhost:3000/register/wizard**

- [ ] ページが 200 で開く (404 / 500 にならない)
- [ ] 「求職者 新規登録」のタイトルが表示
- [ ] 「LINE で 1 タップ登録」ボタンがある
- [ ] **メールアドレス入力欄が 1 個だけ存在**
- [ ] **パスワード入力欄が無い** ← C1 修正の確認
- [ ] 「※ パスワードは最後のステップでお預かりします」の案内文がある
- [ ] 利用規約・プライバシーポリシーへの同意チェックボックスがある
- [ ] 「次へ進む」ボタンがある

### DevTools 確認
F12 → Application タブ → Session Storage → `http://localhost:3000`

- [ ] **`genba-registration-tmp-pw` キーが存在しない** ← C1 修正の確認

---

## 📝 5. ハッピーパス通し検証

### Step 0: 入口
- [ ] メアドに `test+wizard1@example.com` のような自分が受信できるアドレスを入力
- [ ] 同意チェックを入れる
- [ ] 「次へ進む」をクリック
- [ ] `/register/wizard/address` に遷移する

### Step 1: 住まい (`/address`)
- [ ] 進捗バーが表示される (PC 表示)
- [ ] 都道府県セレクタが動く
- [ ] 市区町村が入力できる
- [ ] 「次へ」が有効化される
- [ ] 「次へ」で `/experience` に遷移

### Step 2: 経験 (`/experience`)
- [ ] 建設業 9 カテゴリが表示される
- [ ] カテゴリをクリックするとサブ職種が出る
- [ ] 複数選択できる
- [ ] 経験年数を選べる
- [ ] 経験社数を選べる
- [ ] 「次へ」で `/condition` に遷移

### Step 3: 希望条件 (`/condition`)
- [ ] 「地方ブロック」を開いて都道府県を選べる
- [ ] 最大 5 つまで選択できる (6 個目で警告 or 弾かれる)
- [ ] 選択した数が画面に出る (例: `3/5`)
- [ ] 「次へ」で `/identity` に遷移

### Step 4: 本人情報 (`/identity`) — **最重要**
- [ ] 氏名 (姓・名) の 2 つの入力欄
- [ ] カナ (セイ・メイ) の 2 つの入力欄
- [ ] 携帯番号入力欄
- [ ] **パスワード入力欄が新たに表示される** ← C1 修正の確認
- [ ] 「登録する」ボタンがある (灰色で無効状態)

#### バリデーション動作確認 (1 つずつ試す)

##### 5-A. 氏名・カナ
- [ ] 姓・名を空欄のままだと「登録する」が無効
- [ ] カナに「やまだ」(ひらがな) → 通る
- [ ] カナに「ヤマダ」(カタカナ) → 通る
- [ ] カナに「Yamada」(英字) → 「カタカナ または ひらがなで入力してください」エラー

##### 5-B. 携帯番号 ← C3 修正の確認
- [ ] `0312345678` (固定電話) → 「携帯番号 (070/080/090) を入力してください」エラー
- [ ] `0612345678` (大阪固定) → 同じくエラー
- [ ] `0501234567` (IP 電話) → 同じくエラー
- [ ] `09012345678` (携帯) → エラー消える
- [ ] `090-1234-5678` (ハイフン入り) → エラー消える
- [ ] `０９０１２３４５６７８` (全角数字) → エラー消える
- [ ] `070-1234-5678` → エラー消える
- [ ] `080-1234-5678` → エラー消える

##### 5-C. パスワード
- [ ] 7 文字入力 → 「8 文字以上で入力してください」エラー
- [ ] 8 文字入力 → エラー消える
- [ ] パスワード欄の `type="password"` で値がマスクされる (●●●●)

##### 5-D. 全部正しく入れて登録
- [ ] 氏名・カナ・正しい携帯番号・8文字以上 PW を全て入力
- [ ] 「登録する」が青色で有効化
- [ ] 「登録する」をクリック
- [ ] **クリック直後にボタンが「...」になり連打できない** ← S3 修正の確認
- [ ] 数秒で `/register/wizard/done?emailSent=1` に遷移
- [ ] CheckCircle アイコン + 「ご登録ありがとうございます」が表示
- [ ] 警告バナーが **出ていない** (emailSent=1 だから)
- [ ] 「確認メールを再送する」「先に求人を見てみる」ボタンがある

### Step 5: メール受信
- [ ] 数秒〜2 分以内に登録メアドにメールが届く
- [ ] 件名・本文が崩れていない
- [ ] 確認 URL をクリック → メール認証完了 → ログインできる状態

### Step 6: sessionStorage クリーンアップ確認
DevTools → Application → Session Storage

- [ ] `genba-registration-wizard` キーが消えている (clearAnswers が効いた)
- [ ] `genba-registration-tmp-pw` キーが無い

---

## 🔄 6. エッジケース検証

### 6-A. 戻る/進む耐性 ← S1 修正の確認
- [ ] 新しいシークレットウィンドウで `/register/wizard` を開く
- [ ] Step 1 で都道府県を入力 → 次へ
- [ ] Step 2 で何か選ぶ → ブラウザの「戻る」ボタン
- [ ] Step 1 の入力が **復元されている**
- [ ] もう一度「進む」(ブラウザ右矢印) で Step 2 へ
- [ ] Step 2 の選択も **復元されている**

### 6-B. 別タブ独立性
- [ ] タブ A で `/register/wizard` を開いて Step 2 まで進める
- [ ] **新しいタブ** B で `/register/wizard` を開く
- [ ] タブ B では入力が空 (sessionStorage はタブ独立、これが正常)

### 6-C. 重複登録防止
- [ ] さっき登録したメアドで再度ウィザードを通す
- [ ] Step 4 で「登録する」 → 「このメールアドレスは既に登録されています」エラー
- [ ] 画面が固まらず、再操作できる

### 6-D. 二重送信防止 ← S3 修正の確認
- [ ] 別のメアドで Step 4 まで進む
- [ ] 「登録する」を **連打** する
- [ ] ボタンが即無効になり、1 リクエストしか飛ばない (DevTools → Network で確認)

### 6-E. 旧 sessionStorage 破損耐性 ← S1 修正の確認
- [ ] DevTools → Application → Session Storage で `genba-registration-wizard` の値を `{"corrupted": true}` などに書き換え
- [ ] `/register/wizard` をリロード
- [ ] エラーで真っ白にならず、入口ページが表示される (壊れた値は捨てられる)

---

## 🛑 7. レート制限確認 (任意)

- [ ] 同じ IP から 6 回連続で登録試行
- [ ] 6 回目で 429 「リクエストが多すぎます」エラー

---

## 🧹 8. 検証後の片付け

```bash
# dev サーバを停止 (Ctrl+C)
# テストで作った User を DB から削除する場合:
pnpm prisma studio
# Studio で User テーブルを開き、test+wizard*@example.com の行を削除
```

- [ ] dev サーバ停止
- [ ] 必要ならテストユーザー削除
- [ ] このファイル (PR193_VERIFICATION.md) を削除してよい

```bash
rm PR193_VERIFICATION.md
```

---

## ✅ 検証完了報告

すべてチェックが入ったら、チャットに以下のいずれかを貼ってください:

- 「**全部 OK**」 → PR #193 を Ready 化 → マージへ
- 「**X-Y で問題あり: [説明]**」 → 修正対応

問題があった場合は、以下も添えてください:
- スクリーンショット (あれば)
- ブラウザコンソールのエラー (DevTools → Console タブ)
- ターミナルの `pnpm dev` 側のエラーログ
- 該当 URL
